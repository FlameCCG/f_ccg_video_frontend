# 开发环境默认走 Vite 同源代理 /v1 → 127.0.0.1:8080（见 vite.config.ts）
# 若仍偶发代理空 500，可改为直连后端（需后端 CORS 已开启）：
# VITE_API_BASE=http://127.0.0.1:8080/v1

# 可选：改 Vite 代理目标
# VITE_DEV_PROXY_TARGET=http://127.0.0.1:8080
